import React from "react";
import TakePhoto from "@/components/scan/TakePhoto";
import HandleDiseaseScan from "@/components/scan/HandleDiseaseScan";

type Props = {};

const ScanDisease = (props: Props) => {
  return <HandleDiseaseScan />;
};

export default ScanDisease;
