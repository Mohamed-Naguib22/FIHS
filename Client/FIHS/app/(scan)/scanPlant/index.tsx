import { View, Text } from "react-native";
import React from "react";
import TakePhoto from "@/components/home/TakePhoto";

type Props = {};

const ScanPlant = (props: Props) => {
  return <TakePhoto />;
};

export default ScanPlant;
